# Component maturity

Honest scope, so you know what is production-grade versus reference. AgentGRIT's
value is the governance core; the operator surfaces (API, bot) are reference
integrations you are expected to wire to your own orchestrator.

| Component | Status | Notes |
|---|---|---|
| Bylaw engine (`src/governance/bylaws.py`) | **Solid** | Deterministic, tested. Blocks destructive patterns, escalates risk. |
| Pillar Inspector (`src/governance/pillars.py`) | **Solid** | Deterministic 5-pillar scorecard, tested. |
| Research quality (`src/governance/research_quality.py`) | **Solid** | Deterministic evidence gating + conflict detection (CONTESTED); env-global defaults; optional per-project overrides via `config/projects/<name>.yaml`. Live from `research.culminate`. |
| Decision records (`src/governance/decision_record.py`) | **Solid** | Deterministic; wired into `router.execute`, `research.culminate`, and cost_governor BLOCK/ESCALATE. |
| Autonomy matrix (`src/governance/autonomy.py`) | **Solid** | Risk×Trust×bylaw×evidence gate. **Live + binding** in `router.execute`: `classify_action_risk` + `must_stop` DENY/ESCALATE before any model call; REQUIRE_BRIEFING proceeds after record. |
| Budget governor (`src/governance/budget_governor.py`) | **Solid** | Priority-aware dollar facade; **live** from `cost_governor.govern()` via `check_estimated_usd`. Hard ceiling never rises for priority. |
| Priority manager (`src/governance/priority_manager.py`) | **Solid** | `config/priorities.yaml` weights; live via budget_governor + cost_governor. |
| Config loader (`src/governance/config_loader.py`) | **Solid** | `config/budget.yaml` + `priorities.yaml`; defaults identical to historical GovernorConfig/env when files absent. |
| Daily debrief (`src/agents/daily_debrief_agent.py`) | **Solid** | Deterministic rollup; **schedulable** via `make debrief` / `run_debrief_and_notify` (optional notify). Public repo has no GM cron — wire from your scheduler. |
| Idea→project (`src/planning/idea_to_project.py`) | **Solid** | Offline scaffold (`projects/<slug>/`); CLI + `make idea-project`. |
| Skill discovery (`src/execution/skill_discovery.py` + `skills/`) | **Solid** | Drop-in `skills/` catalog; `discover_local` + CLI; propose-only (no install). |
| **Repo Steward** (`src/agents/repo_steward_agent.py`) | **Solid** | First turnkey agent: composes `gardener.tend` + `discover_local` + autonomy gate + one `decision_record` per run. **Governed advisor — never auto-edits.** Live via `make agent-steward`, CLI, and `python -m src.main --agent repo_steward`. Sample run: [EXAMPLE-REPO-STEWARD.md](EXAMPLE-REPO-STEWARD.md). |
| **Legal Research Advisor** (`src/agents/legal_research_agent.py` + `src/observe/adapters/courtlistener.py`) | **Solid** | Governed attorney-tool: CourtListener public-record opinions, **cite-or-refuse**, `research_quality.assess` (CONTESTED on split authority), autonomy gates FILE/SEND/ADVISE-CLIENT, one `decision_record` per run. **UPL firewall** refuses non-attorney advice framing. **Never files/serves/sends.** Not legal advice. CLI `make agent-legal Q=…`, `python -m src.agents.legal_research_agent`. Sample: [EXAMPLE-LEGAL-RESEARCH.md](EXAMPLE-LEGAL-RESEARCH.md). Optional `COURTLISTENER_TOKEN` for higher API limits. |
| **GRIT Observe** (`src/observe/`) | **Solid** | Governed live-data OBSERVE layer: keyless adapters (USGS, GDELT, Polymarket) → fuse (dedupe + freshness + cross-source) → `research_quality.assess` + one `decision_record` per run. **Never acts** — stale/lone-source/contested cannot be actionable. CLI `make observe`, API `GET /observe/view`. Sample: [EXAMPLE-OBSERVE.md](EXAMPLE-OBSERVE.md). |
| Research layer (`src/execution/research.py`, `research_providers.py`) | **Solid** | Free-first, provider-agnostic, budget-governed. DuckDuckGo works keyless; add your own provider keys. |
| Deliberation (`src/governance/deliberation.py`) | **Working** | JR→Manager local-model review; requires a local model (Ollama). Fails safe to escalate. |
| Trust ladder (`src/governance/trust.py`) | **Working** | State machine present; promotion/demotion policy is conservative by default. |
| Cost-first router (`src/execution/router*.py`) | **Solid** | Capability-based routing + binding autonomy; tested. |
| Notifications (`src/utils/notify.py`) | **Working** | Pluggable channels (Telegram/webhook/command); opt-in. |
| FastAPI server (`src/api/server.py`) | **Working** | Task orchestration endpoints return explicit 501 (honest-disabled, no fake queued/running state); remaining endpoints are scaffolding for your orchestrator, but guarded by an `X-API-Key` dependency that fails closed — it refuses to serve on a non-loopback host unless `API_SECRET_KEY` is set. See SECURITY.md. |
| **Ops console** (`GET /console`, `/console/data?screen=…`) | **Solid (read-only multi-screen console)** | Multi-screen ops UI: Overview · Tasks · Governance · Research · Models & Cost · Audit. Rolls up existing JSONL only; **never POSTs / never acts** (approvals stay CLI/Telegram). Flat rollup kept for back-compat. Pillars/projects panes honest-thin without dedicated logs. Sample: [EXAMPLE-CONSOLE.md](EXAMPLE-CONSOLE.md). |
| **Governed brief UI** (`GET /brief`, `/brief/data`) | **Solid (read-only governed-brief UI)** | Domain-user briefing: normalized `GovernedBrief` from legal_research / repo_steward / observe adapters; **verified citations only**; CONTESTED + judgment flags; render **profiles** (generic default; legal sample override). `logs/briefs.jsonl` via `brief_record` (agents write; UI reads). Sample: [EXAMPLE-BRIEF.md](EXAMPLE-BRIEF.md). |
| **Video capability runtime** (`src/video/`) | **Reference (Phase 1 scaffold)** | Model-agnostic redesign of claude-video *ingestion* lessons: `VideoEvidenceBundle` schema, Class A/B/C registry, privacy wall + route refuse, fixture pipeline. **No LLM “watches video” yet** — evidence plane first. Spec: [VIDEO-CAPABILITY-RUNTIME.md](VIDEO-CAPABILITY-RUNTIME.md). |
| **GRIT Sentinel** (`src/sentinel/`) | **Working (Phase Zero: scanner)** | Fail-closed static scans (secrets, dangerous code, prompt-injection markers, egress) with ALLOW/LIMIT/HOLD/BLOCK verdicts, masked excerpts, sha256 evidence, append-only `logs/sentinel.jsonl`. **Capability-broker enforcement is the named next phase — nothing here pretends to enforce yet.** |\n| **Cells Interlinked** (`src/cells/`) | **Working (contract + validator)** | Typed 12-cell context envelope with selective overlays and interlock validation (completion gated on evidence + passing checkpoint; claims need references or labels; delegation needs bounds). Runtime wiring into task flows is the next phase. |\n| **GRIT Sentinel** (`src/sentinel/`) | **Working (Phase Zero: scanner)** | Fail-closed static scans (secrets, dangerous code, prompt-injection markers, egress) with ALLOW/LIMIT/HOLD/BLOCK verdicts, masked excerpts, sha256 evidence, append-only `logs/sentinel.jsonl`. **Capability-broker enforcement is the named next phase — nothing here pretends to enforce yet.** |\n| **Cells Interlinked** (`src/cells/`) | **Working (contract + validator)** | Typed 12-cell context envelope with selective overlays and interlock validation (completion gated on evidence + passing checkpoint; claims need references or labels; delegation needs bounds). Runtime wiring into task flows is the next phase. |\n| **AgentGRIT Desktop** (`GET /desktop`) | **Working (read-only desktop shell)** | Windowed command environment: Mission Control, Agent Room, Approval Center (anatomy: risk, blast radius, reversibility, evidence, alternatives), Evidence Inspector, Routing+Cost ledger with postures, Historian, Artifacts, Policy+Trust. Dual mode: LIVE off `GET /console/data` when served by the API, labeled demo state standalone. Self-contained, GET-only, **never acts**. |\n| **Logos Vault consumer** (`src/logos_vault/`) | **Working (contract + fail-closed local retrieval; no model wiring)** | Governed shared reasoning-corpus consumer: disabled by default; pin consistency + per-artifact sha256 integrity; admission states; role profiles (GRIT JR / GRIT / GRIT GM); byte-budgeted untrusted-context bundles. **No agent call site loads vault context automatically yet.** Spec: [LOGOS-VAULT.md](LOGOS-VAULT.md). |
| Telegram bot (`src/bot/telegram.py`) | **Reference** | Command surface scaffolding — wire spawn/status handlers to your orchestrator. Hardened bot remains the `main.py` control plane. |
| Reply capture | **Not included** | Reading human replies is platform-specific (e.g. macOS Messages). The notify/ask direction is pluggable; implement capture for your channel. |

"Solid" = deterministic and unit-tested. "Working" = functional, may need your
config (local model, keys). "Reference" = a starting point you complete for your
stack. Do not expose the API without reading `SECURITY.md`.

How the pieces move together — task lifecycle, trust ladder, and per-project
doctrine flow, with each node mapped to its module: [DIAGRAMS.md](DIAGRAMS.md).
